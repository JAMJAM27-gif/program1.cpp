#include <iostream> 
using namespace std;

int main() {
    int exitTime, entryTime, period
    float change;
    cout<<"Please enter your entry time: ";
    cin>>entryTime;
    cout<<"Please enter your exit time: ";
    cin>>exitTime

    period = exitTime - entryTime;
    if(period > 1)
        charge = (period) + 1;
    else
        charge = 2;
    cot<<"The charge for yourparking  is "<<charge;
}